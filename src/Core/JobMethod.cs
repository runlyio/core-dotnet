namespace Runly
{
	public enum JobMethod
	{
		InitializeAsync,
		GetItemsAsync,
		Count,
		GetEnumerator,
		EnumeratorMoveNext,
		EnumeratorCurrent,
		GetItemIdAsync,
		ProcessAsync,
		FinalizeAsync
	}
}
